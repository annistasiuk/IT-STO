# Програма для менеджменту транспортів на СТО

🔧 **Проєкт IT-STO**

**Мета:** створення веб-додатку для автоматизації обліку клієнтів, транспортних засобів і замовлень на СТО.

---

## 🧠 Технологічний стек

| Компонент | Технологія | Опис |
|-----------|------------|------|
| Backend | Django | Основний фреймворк для побудови API та адмін-панелі |
| Database | SQLite (на етапі MVP) | Проста локальна БД для швидкого тестування |
| Frontend | HTML, CSS, JS | Базовий інтерфейс для користувачів |
| Контроль версій | Git + GitHub | Версіонування коду |
| Керування проєктом | Trello | Візуальне планування завдань |

---

## ⚙️ Структура застосунків
```python
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'cars',
    'repairs',
    'billing',
    'notifications',
]
```

---

## 🚀 Як запустити проєкт

Для того, щоб запустити цей проєкт на локальному комп'ютері, вам знадобиться встановлений Python (версія 3.10 або новіша) та Git.

### Покрокова інструкція:

**1. Клонуйте репозиторій:**
Відкрийте термінал та введіть команду (або просто завантажте архів з кодом):
```bash
git clone <посилання-на-ваш-гітхаб>
cd sto_management
```

**2. Створіть віртуальне середовище:**
Це ізольоване місце для бібліотек проєкту.
```bash
python -m venv .venv
```

**3. Активуйте віртуальне середовище:**

* **Windows:**
```bash
.venv\Scripts\activate
```

* **MacOS / Linux:**
```bash
source .venv/bin/activate
```

(Після активації у терміналі з'явиться напис `(.venv)`)

**4. Встановіть необхідні бібліотеки:**
Всі бібліотеки прописані у файлі `requirements.txt`. Встановіть їх однією командою:
```bash
pip install -r requirements.txt
```

**5. Створіть базу даних:**
Ця команда створить файл `db.sqlite3` та налаштує структуру таблиць.
```bash
python manage.py migrate
```

**6. Створіть адміністратора (Суперкористувача):**
Щоб мати доступ до панелі керування. Введіть логін та пароль за вказівками.
```bash
python manage.py createsuperuser
```

**7. Запустіть сервер:**
```bash
python manage.py runserver
```

---

## 🌍 Доступ до сайту

Після запуску відкрийте браузер:

* **Головна сторінка:** http://127.0.0.1:8000/